name: Build QEMU 8.2.5 (AArch64) for Android

on:
  workflow_dispatch:

jobs:
  build-qemu:
    runs-on: ubuntu-latest

    steps:
      - name: 1. 저장소 코드 체크아웃
        uses: actions/checkout@v4

      - name: 2. 안드로이드 SDK 및 NDK 세팅
        uses: android-actions/setup-android@v3
        with:
          cmdline-tools-version: 8512546

      - name: 3. NDK (r25c) 직접 설치
        run: |
          sdkmanager --install "ndk;25.2.9519653"

      - name: 4. 빌드 필수 패키지 설치
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            python3 \
            python3-pip \
            ninja-build \
            pkg-config \
            libglib2.0-dev \
            libpixman-1-dev \
            flex \
            bison

      - name: 5. QEMU 8.2.5 소스코드 다운로드 및 압축 해제
        run: |
          wget https://download.qemu.org/qemu-8.2.5.tar.xz
          tar -xf qemu-8.2.5.tar.xz

      - name: 6. Android ARM64용 QEMU (aarch64-softmmu) 크로스 컴파일
        run: |
          cd qemu-8.2.5
          
          # NDK 경로 자동 탐색 설정 (ndk;25.2.9519653 = NDK r25c)
          export NDK=$ANDROID_HOME/ndk/25.2.9519653
          export TOOLCHAIN=$NDK/toolchains/llvm/prebuilt/linux-x86_64
          export TARGET=aarch64-linux-android
          export API=29
          
          export CC=$TOOLCHAIN/bin/${TARGET}${API}-clang
          export CXX=$TOOLCHAIN/bin/${TARGET}${API}-clang++
          export AR=$TOOLCHAIN/bin/llvm-ar
          export LD=$TOOLCHAIN/bin/ld
          export RANLIB=$TOOLCHAIN/bin/llvm-ranlib
          export STRIP=$TOOLCHAIN/bin/llvm-strip
          
          # QEMU 빌드 옵션 구성
          ./configure \
            --cross-prefix=${TARGET}- \
            --target-list=aarch64-softmmu \
            --disable-gtk \
            --static
            
          # 병렬 컴파일 실행
          make -j$(nproc)

      - name: 7. 빌드 결과물 업로드 (Artifacts)
        uses: actions/upload-artifact@v4
        with:
          name: qemu-8.2.5-android-aarch64
          path: |
            qemu-8.2.5/build/qemu-system-aarch64
            qemu-8.2.5/build/qemu-img
